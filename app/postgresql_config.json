{
    "role": {
        "role_name": "postgres",
        "role_password": "cv251181ojv"
    },
    "database": {
        "database_name": "store_db"
    },
    "createDatabase": {
        "createDatabase": "createdb store_db"
    },
    "createTables": {
        "createCategoriesTable": "CREATE TABLE categories (id serial, category_name varchar(50) NOT NULL, category_id serial, category_position_in_list bigint NOT NULL, category_description text, category_start_date varchar(200), category_image oid, category_parent_id bigint REFERENCES categories (category_id)  ON UPDATE CASCADE  ON DELETE CASCADE, CONSTRAINT pk_category_id PRIMARY KEY (category_id), CHECK (category_position_in_list > 0));",
        "createProductsTable": "CREATE TABLE products (id serial, product_id serial, product_position_in_list bigint NOT NULL, product_display bool DEFAULT(FALSE), product_parent_id bigint REFERENCES categories(category_id) ON UPDATE CASCADE ON DELETE CASCADE NOT NULL, product_fullname varchar(10000) NOT NULL, product_shortname varchar(2000) NOT NULL,product_description text NOT NULL, product_specification text, product_pricesType json NOT NULL, product_price varchar(200) NOT NULL, product_photosUrls text NOT NULL, product_discounts text, product_discountsValues json, product_brand_id bigint REFERENCES brands(brand_id) ON UPDATE CASCADE  ON DELETE CASCADE, product_stock bool DEFAULT(FALSE), product_quantityStock bigint DEFAULT(0), product_quantityOrders bigint DEFAULT(0), product_quantitySendItems bigint DEFAULT(0), product_quantityPaidItems bigint DEFAULT(0), product_raiting float4 DEFAULT(0), product_barcode bigint, product_start_date varchar(200) NOT NULL, product_end_date varchar(200), CONSTRAINT pk_product_id PRIMARY KEY ( product_id ), CHECK (product_position_in_list > 0), CHECK (product_quantityStock >= 0), CHECK (product_quantityOrders >= 0), CHECK (product_quantityPaidItems >= 0), CHECK (product_quantitySendItems >= 0));",
        "createSliderTable": "CREATE TABLE slider (id serial, slider_position_in_list bigint NOT NULL, slider_name varchar(2500), slider_description varchar(2500), slider_url varchar(2500), slider_display bool DEFAULT(FALSE), slider_id serial, slider_image_name varchar(1000) NOT NULL UNIQUE, slider_image oid NOT NULL, CONSTRAINT pk_slider_id PRIMARY KEY (slider_id));",
        "createBrandsTable": "CREATE TABLE brands (id serial, brand_id serial, brand_name varchar(1000), brand_photo oid, brand_description varchar(5000), brand_url varchar(5000), CONSTRAINT pk_brand_id PRIMARY KEY (brand_id));",
        "createCurrenciesTable": "CREATE TABLE currencies (id serial, currency_id serial, currency_country varchar(1000) NOT NULL, currency_iso_code varchar(20) NOT NULL, currency_iso_number_code bigint NOT NULL, currency_numeric_code varchar(20) NOT NULL, currency_value float4 NOT NULL, currency_default bool DEFAULT(FALSE), currency_last_update varchar(500) NOT NULL, CONSTRAINT pk_currency_id PRIMARY KEY (currency_id), CHECK (currency_value > 0));",
        "createCurrenciesHistoryTable": "CREATE TABLE currenciesHistory (id serial, currency_history_id serial, currency_parent_id bigint REFERENCES currencies (currency_id)  ON UPDATE CASCADE  ON DELETE CASCADE, currency_history_date_update bigint NOT NULL, currency_history_value float4 NOT NULL, currency_history_difference float4 NOT NULL, CONSTRAINT pk_currency_history_id PRIMARY KEY (currency_history_id), CHECK (currency_history_value > 0));"
    }
}